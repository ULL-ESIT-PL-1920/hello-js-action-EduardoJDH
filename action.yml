name: 'Hello World'
description: 'Saludar'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  time: # id of output
    description: 'La hora'
runs:
  using: 'node12'
  main: 'index.js'
